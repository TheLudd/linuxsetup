alias apap="sudo apt-get update && sudo apt-get upgrade && sudo apt-get autoremove && sudo apt-get autoclean"
alias e=". doexit"
alias l="ls -l"
alias ll="ls -la"
alias nt="gnome-terminal"
alias mdir=". makedir"
alias rebase="git rebase -i HEAD~"
alias upnpm="sudo npm i -g npm && npm completion | sudo tee /etc/bash_completion.d/npm > /dev/null && npm -v "
alias mt="cd ~/mediatool && zsh"
alias clock="killall unity-panel-service"
alias mount-eric="sshfs eric:/var/lib/transmission-daemon/downloads ~/eric && gnome-open ~/eric"
alias mount-external="sshfs transmission:/var/lib/transmission-daemon/downloads ~/eric && gnome-open ~/eric"
alias pyser="python -m SimpleHTTPServer"
alias hack=". createhack"
alias peace="git st | grep -P 'both (added|modified)' | sed -r 's/both .*:\s*(.*)$/\1/g' | xargs vi -p"
alias mouse="sudo modprobe -r psmouse && sudo modprobe psmouse"
alias vip="xargs vi -p"
