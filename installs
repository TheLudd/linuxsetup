#!/bin/bash
sudo apt-get update
sudo apt-get install -y vim htop tree zsh

# git extras
(cd /tmp && git clone --depth 1 https://github.com/tj/git-extras.git && cd git-extras && sudo make install)

./setupdotfiles
